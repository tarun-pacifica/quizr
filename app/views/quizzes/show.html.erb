<h3><%= @quiz.name %></h3>

<% if @current_user.user_quizzes.where(:quiz_id => @quiz.id).first.complete? %>
  <%= render 'completed_message' %>
<% else %>
  <%= render 'user_answers/form' %>
<% end %>